spring:
  application:
    name: esports-platform

  servlet:
    multipart:
      max-file-size: 15MB
      max-request-size: 15MB
  
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SPRING_MAIL_USERNAME:esportsarena638@gmail.com}
    password: ${SPRING_MAIL_PASSWORD:lrslghrqdrtkbbim}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 3000
          timeout: 3000
          writetimeout: 3000
  
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/test?useSSL=true&requireSSL=false&verifyServerCertificate=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${SPRING_DATASOURCE_USERNAME:3HGZktJF7kMeoJ1.root}
    password: ${SPRING_DATASOURCE_PASSWORD:<PASSWORD>}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 300000
      connection-timeout: 20000
      max-lifetime: 1200000
      initialization-fail-timeout: 0

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate # We use flyway for migrations
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        type:
          preferred_uuid_jdbc_type: VARCHAR

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    repair-on-migrate: true

server:
  port: ${PORT:8080}

app:
  jwt:
    secret: ${JWT_SECRET:9a4f2c8d3b7a1e6f45c8a0b3f267d8b1d4e6f3c8a9d2b5f8e3a9c8b5f6v8a3d9}
    expiration: 86400000 # 24 hours
  razorpay:
    key-id: ${RAZORPAY_KEY_ID:rzp_test_pUrj47p2o1XQ6H}
    key-secret: ${RAZORPAY_KEY_SECRET:dummy_secret}
  brevo:
    api-key: ${BREVO_API_KEY:}
  frontend-url: ${APP_FRONTEND_URL:http://localhost:5173}


